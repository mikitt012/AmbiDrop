*.pt filter=lfs diff=lfs merge=lfs -text
*.pth.tar filter=lfs diff=lfs merge=lfs -text

# Precomputed steering matrices (utils/steering) via Git LFS — large .mat files
utils/steering/*.mat filter=lfs diff=lfs merge=lfs -text
